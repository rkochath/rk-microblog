<!-- extend base layout -->
{% extends "base.html" %}

{% block content %}

{% include 'flash.html' %}
<div class="well">
    <form class="form-horizontal" action="" method="post" name="post">
        {{form.hidden_tag()}}
        <div class="control-group{% if form.errors.post %} error{% endif %}">
            <label class="control-label" for="search">{{ _('Search:') }}</label>
            <div class="controls">
                {{ form.search(size = 30, maxlength = 140) }}
                {% for error in form.errors.search %}
                    <span class="help-inline">[{{error}}]</span><br>
                {% endfor %}
            </div>
        </div>
        <div class="control-group">
            <div class="controls">
                <input class="btn" type="submit" value="{{ _('Search') }}">
		<a href="{{ url_for('rateinput', contract_id=0) }}">   <input class="btn" type="button" value="{{ _('New') }}"> </a>
            </div>
        </div>
    </form>
</div>

<body>  
<table class="table">  
        <thead>  
          <tr>  
            <th>Contract Description</th>  
            <th>Start Date</th>  
            <th>End Date</th>  
            <th>Total Contract Value</th>  
          </tr>  
        </thead>  
        <tbody>  

		{% for contract in pagination.items %}
		  <tr><td><a href="{{ url_for('rateinput', contract_id = contract.id) }}">{{ contract.description }}</td><td>{{ contract.start_date }}</td><td>{{ contract.end_date }}</td><td>{{ contract.income }}</td></a></tr>
		{% endfor %}

        </tbody>  
      </table>  
</body>  

<ul class="pager">
    {% if pagination.has_prev %}
    <li class="previous"><a href="{{ url_for('contracts_list', page = pagination.prev_num) }}">{{ _('Newer Contracts') }}</a></li>
    {% else %}
    <li class="previous disabled"><a href="#">{{ _('Newer Contracts') }}</a></li>
    {% endif %}
    {% if pagination.has_next %}
    <li class="next"><a href="{{ url_for('contracts_list', page = pagination.next_num) }}">{{ _('Older Contracts') }}</a></li>
    {% else %}
    <li class="next disabled"><a href="#">{{ _('Older Contracts') }}</a></li>
    {% endif %}
</ul>


{% endblock %}
